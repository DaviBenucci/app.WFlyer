# Design: brand identity foundation

The root `brand/` directory is the structured asset workspace. `docs/brand/` contains the decision process. `brand-manifest.yaml` is the machine-readable state used by agents and validators.

While the state is `pending`, prototypes and future UI use text-only naming. Existing visual-system tokens remain provisional product-interface tokens and are not declared as the corporate brand palette.
