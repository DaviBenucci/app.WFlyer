# Design

The change is documentation-only. It introduces five bounded documentation areas: company, billing, fiscal, infrastructure and operations. All commercial/fiscal decisions remain proposed. The company website and client workloads are kept outside the SaaS repository and production account. DNS maps `app.wflyer.com.br` to AWS instead of changing the visible URL by redirect.
