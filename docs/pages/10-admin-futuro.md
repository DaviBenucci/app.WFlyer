# Painel administrativo — fora do MVP

Painel administrativo não faz parte do MVP inicial.

Não implementar rota, navegação, componente, API ou permissão administrativa nesta etapa.

Dados internos devem permanecer fora dos DTOs públicos e aparecer apenas em logs controlados com `correlation_id`.
